package modules
/*
import communication.request.NormalRequestType
import communication.request.normal.NormalRequest
import deserializer.*
import deserializer.request.*
import org.koin.core.qualifier.named
import org.koin.dsl.module

val normalRequestDeserializerModule = module{
    single<Deserializer>(named(NormalRequestType.DESCRIPTION)){ DescriptionRequestDeserializer() }
    single<Deserializer>(named(NormalRequestType.EXECUTION)){ ExecutionRequestDeserializer() }
    single<Deserializer>(named(NormalRequestType.AUTHORIZATION)){ AuthorizationRequestDeserializer() }
    single<Deserializer>(named(NormalRequestType.REGISTRATION)){ RegistrationRequestDeserializer() }
}

val actionRequestDeserializerModule = module{

}

 */